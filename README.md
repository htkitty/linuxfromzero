## Have you ever wanted to build a clean, minimalist distro from the ground up?
if so, you might enjoy LF0.

### LF0 Doesn't use systemd
We rely heavily on a custom Init script for the kernel to function.

### Traditional LF0 counts on lightweight utils]
being such as busybox, which is the default LF0 shell.
